// В файле arrays-2.js создай массив с десятью элементами строкового типа. 
// Ниже напиши цикл, который выведет все элементы в консоль. 

let mass = ["a", "c", "b", "d", "w", "a", "p", "g", "v", "l"]  

for(i = 0; i < mass.length; i++){
   console.log(mass[i])
}
